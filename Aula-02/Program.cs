using System;

class Program{
    static void Main(string [] args){
        Console.WriteLine("buceta de C# do caralho quero um curso atualizado buceta"); //editando o hello world
        if(args.GetLength(0)>0){
         Console.Write(args.GetValue(0)); //criado uma estrutura condicional para usar os argumentos de entrada na hora de executar o programa
        }
    }
}

// arquivo de C# criado em base no dotnet